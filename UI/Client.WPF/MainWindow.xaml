<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:Fap.Presentation" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Class="Fap.Presentation.MainWindow"
    x:Name="Window"
    Title="{Binding WindowTitle}"
    mc:Ignorable="d"
    Icon="/Fap.Presentation;component/Images/folder-yellow.ico" WindowStartupLocation="Manual"
    MinWidth="260" MinHeight="400" ShowInTaskbar="True"
    Width="350" Height="900">
    <Window.Resources>
        <local:SessionTypeConverter x:Key="SessionTypeConverter" />
    </Window.Resources>
    <Grid x:Name="LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="4*" />
            <RowDefinition Height="2*" MinHeight="100" />
            <RowDefinition Height="*" MinHeight="100" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="8,8,8,4">
            <Label Content="{Binding NodeStatus}" Padding="2,1" />
            <Label Content="{Binding CurrentNetworkStatus}" Padding="2,1" />
            <Label Content="{Binding LocalStats}" Padding="2,1" />
            <Label Content="{Binding GlobalStats}" Padding="2,1" />
            <WrapPanel Margin="0,4,0,0">
                <Button Margin="2" Command="{Binding Search}" Content="Search" Padding="8,4" />
                <Button Margin="2" Command="{Binding ViewQueue}" Content="Transfers" Padding="8,4" />
                <Button Margin="2" Command="{Binding EditShares}" Content="Shares" Padding="8,4" />
                <Button Margin="2" Command="{Binding Compare}" Content="Compare" Padding="8,4" />
                <Button Margin="2" Command="{Binding Settings}" Content="Settings" Padding="8,4" />
            </WrapPanel>
        </StackPanel>

        <DockPanel ContextMenuOpening="DockPanel_ContextMenuOpening" Grid.Column="0" Grid.Row="1" LastChildFill="True">
            <ListBox ItemContainerStyle="{DynamicResource PeerListBoxItemPresenter}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Margin="2,2,2,2" x:Name="clientListBox" DockPanel.Dock="Right" ItemsSource="{Binding Peers}" MouseDoubleClick="listBox1_MouseDoubleClick" SelectedItem="{Binding SelectedClient}">
                <ListBox.ContextMenu>
                    <ContextMenu>
                    </ContextMenu>
                </ListBox.ContextMenu>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Image Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" VerticalAlignment="Top" Height="35px" Source="{Binding Avatar, Converter={StaticResource imageConverter}}" Margin="0,0,0,0" />
                            <Label Grid.Column="1" Grid.Row="0" Content="{Binding Nickname}" Margin="0,0,0,0" />
                            <Label Grid.Column="1" Grid.Row="1" Content="{Binding Host}" Margin="0,0,0,0" />
                            <Label Grid.Column="2" Grid.Row="0" Content="{Binding ShareSize, Converter={StaticResource sizeConverter}}" />
                            <Label Grid.Column="2" Grid.Row="1" Content="{Binding Description}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>

        <DockPanel Grid.Row="2">
            <DockPanel DockPanel.Dock="Bottom">
                <Button x:Name="sendText" DockPanel.Dock="Right" Width="70px" Command="{Binding SendChatMessage}" Content="Send"/>
                <TextBox x:Name="chatTextBox" Text="{Binding CurrentChatMessage}" Margin="2" KeyDown="inputText_KeyDown" />
            </DockPanel>
            <ListBox x:Name="chatBox" ItemsSource="{Binding ChatMessages}" local:ListBox.AutoScroll="True" VirtualizingStackPanel.IsVirtualizing="True" ItemContainerStyle="{DynamicResource ListBoxItemStyle1}" />
        </DockPanel>

        <DockPanel Grid.Row="3">
            <ListBox ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto" ItemsSource="{Binding Sessions}" HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <ProgressBar BorderThickness="0" Value="{Binding Percent}" Style="{DynamicResource TransferProgressBar}">
                            <ProgressBar.Width>
                                <Binding Path="ActualWidth"
                                         RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType={x:Type ScrollContentPresenter}}" />
                            </ProgressBar.Width>
                        </ProgressBar>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Grid>
</Window>
